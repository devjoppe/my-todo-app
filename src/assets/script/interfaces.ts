export interface todosItem {
    todo: string,
    completed: boolean,
    id: any,
    category: string,
    created: any,
    userid: string
}

export interface userItem {
    name: string,
    userid: any
}