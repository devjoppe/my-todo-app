export interface todosItem {
    todo: string,
    completed: boolean,
    id: any,
    category: string,
    created: any,
    userid: string
}

export interface userItem {
    username: string,
    email: string,
    id: any
}